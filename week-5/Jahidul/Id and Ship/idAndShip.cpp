// @JahidulZaid
#include <bits/stdc++.h>
using namespace std;
#define optimize() ios_base::sync_with_stdio;cin.tie(0);cout.tie(0);
#define endl '\n'

int main() {
	// your code goes here
	optimize();
	int t;
	cin >> t;
	while(t--){
	    char a;
	    cin >> a;
	    
	    if( a == 'b' || a =='B' ) cout<<"BattleShip"<<endl;
	    if( a == 'c' || a =='C' ) cout<<"Cruiser"<<endl;
	    if( a == 'd' || a =='D' ) cout<<"Destroyer"<<endl;
	    if( a == 'F' || a =='f' ) cout<<"Frigate"<<endl;
	    
	    
	   
	}

    
	return 0;
}
 